<Page x:Class="GestorUsuarios.views.AddTask"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GestorUsuarios.views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddTask">


    <Grid Background="White">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Width="350">
            <TextBlock Text="Asignar Tarea" FontSize="24" FontWeight="Bold" Margin="10" HorizontalAlignment="Center"/>

            <!--Nombre de tarea -->
            <TextBlock Text="Nombre de la tarea:" FontSize="14"/>
            <TextBox Name="NameTask" Width="250"/>

            <!-- lista de usuarios -->
            <TextBlock Text="Asignar a:" FontSize="14"/>
            <ComboBox Name="userComboBox" Width="250"/>

            <!-- prioridad -->
            <TextBlock Text="Prioridad:" FontSize="14"/>
            <ComboBox Name="priorityComboBox" Width="250"/>

            <!-- Especifiaciones -->
            <TextBlock Text="Especifiaciones:" FontSize="14"/>
            <TextBox Name="specifications" Width="250"/>

            <!-- Plantilla correo -->
            <TextBlock Text="Pantilla Correo:" FontSize="14"/>
            <ComboBox Name="templayEmailComboBox" Width="250"/>

            <!-- Botón para confirmar -->
            <Button Content="Asiganar tarea" Width="200" Margin="10" Click="createTask"/>
        </StackPanel>
    </Grid>
</Page>
